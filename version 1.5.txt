==========================================
  VoxCap — Version 1.5
==========================================

Ye file batati hai ki aapke paas kaunsa version hai.
Latest version yahan se milta hai:
https://github.com/soubickdas-lab/VoxCap/releases/latest/download/VoxCap-share.zip
Update karne ke liye bas update.bat double-click karo.

------------------------------------------
FEATURES
------------------------------------------
- Audio/Video → CapCut-style SRT captions (fully offline, Whisper large-v3)
- Meaningful line breaks — har caption line ka apna matlab banta hai
  (comma, connector words, pause par break; kabhi mid-phrase cut nahi)
- SRT Matcher mode — apna script/beats do, har line audio se time-match hoti hai
- Drop karte hi auto-transcribe, SRT auto-download + live preview
- Hindi, English + 99 languages, auto-detect
- NVIDIA GPU par batched inference (full GPU usage, ~5x fast)
- GPU na ho to CPU par medium model se kaam chalta hai
- Badi files (GB+) chunked upload se — Cloudflare 100MB limit ka fix
- Start / Stop buttons — Stop turant pura process band karta hai
- Caption length slider (40–90 characters)

------------------------------------------
CHANGELOG
------------------------------------------

v1.5 (12 Aug 2026)
- Audio/Video player result me — SRT banne ke baad wahi play karke check karo
- Live caption box player ke upar — playback ke saath real-time me dikhta
  hai ki SRT us waqt kya keh rahi hai (audio-SRT sync verify karne ke liye)
- Fill Gaps ab toggle hai — dobara click karo to original timings wapas
- Gap-filled SRT download hone par filename ke end me "gap-filled" likha
  aata hai (jaise "myvideo gap-filled.srt")

v1.4 (8 Aug 2026)
- Live system monitor UI ke top-right me: CPU / GPU / RAM usage % —
  har 2 second update, color coding (green < 60%, yellow < 85%, red 85%+)

v1.3 (7 Aug 2026)
- SRT Matcher boundaries ekdum precise: har line audio me wahi se shuru
  hoti hai jahan uska pehla word bola gaya, aur wahi khatam jahan aakhri
  word khatam hua — chahe transcription me spelling alag ho
- Timing accuracy fix: default ab PRECISE mode hai — word timestamps
  100% accurate (batched fast mode timestamps aage-peeche karta tha).
  Fast mode wapas chahiye to VOXCAP_BATCH=16 env var set karo
- Fill Gaps button se hota hai (automatic nahi), aur ab sahi direction me:
  har caption ka END aage badh ke agli caption ke START tak jata hai
  (caption screen par rehta hai jab tak agla na aaye) — start points
  original rehte hain, pehli caption 00:00:00,000 se

v1.2 (7 Aug 2026)
- Fill Gaps button (dono modes me) — har caption ka start pichhli caption
  ke end se jud jata hai, pehli caption 00:00:00,000 se shuru hoti hai.
  SRT me koi khaali gap nahi bachta, caption audio se late nahi dikhta
- SRT Matcher zyada accurate — fuzzy matching se transcription ki chhoti
  galtiyan (spelling/word endings) bhi match ho jati hain

v1.1 (7 Aug 2026)
- CapCut jaisi lambi caption lines (~70 chars) — chhote 4-word chunks hataye
- Meaningful breaks: comma/clause ke baad, when/and/but/ki/lekin jaise
  words se pehle, ya saans ke pause par — dono lines balanced
- GPU batched inference — transcription ~5x fast, full GPU utilization
- Batched mode ka punctuation bug fix (lowercase/bina full-stop wala issue)
- Stop button ab sach me pura process kill karta hai (127ms me)
- Record Mic button hataya
- SRT Matcher ka demo text story-script style ka
- Version file system (ye file!)

v1.0 (7 Aug 2026)
- Pehla release: FastAPI + faster-whisper large-v3 (CUDA)
- Browser UI, SRT download + preview, SRT Matcher, mic recording
- Cloudflare tunnel se srt.aipoint.online par live
- Portable setup: setup.bat, start-local.bat, update.bat, GitHub release
